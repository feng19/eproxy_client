{
    application,
    eproxy_client,
    [
        {description, "eproxy_client"},
        {vsn, "0.4.0"},
        {registered, [
            epc_sup,
            epc_http_sup,
            epc_socks4_sup,
            epc_socks5_sup
        ]},
        {applications, [kernel, stdlib, crypto, ssl]},
        {mod, {epc_app, []}},
        {modules, []}
    ]
}.
